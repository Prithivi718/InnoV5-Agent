current_capacity = None
required_capacity = None

def text_prompt(msg):
  try:
    return raw_input(msg)
  except NameError:
    return input(msg)


current_capacity = text_prompt('')
required_capacity = text_prompt('')
if current_capacity < required_capacity:
    print('preserve capacity for final correction')
