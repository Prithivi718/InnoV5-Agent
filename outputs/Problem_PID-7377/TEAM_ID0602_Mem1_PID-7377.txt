drift = None
threshold = None

def text_prompt(msg):
  try:
    return raw_input(msg)
  except NameError:
    return input(msg)


drift = float(text_prompt(''))
threshold = float(text_prompt(''))
if drift > threshold:
    print('Explosion')
